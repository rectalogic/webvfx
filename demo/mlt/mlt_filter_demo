#!/usr/bin/env bash

export DISPLAY=${DISPLAY:-:0}
melt -verbose "${VFX_SOURCE:-red.mlt}" out=299 \
    -filter webvfx:../examples/filter-demo.qml out=299 \
    $(eval echo $(< "${VFX_CONSUMER:-consumer_sdl}")) mlt_profile=webvfx_profile
