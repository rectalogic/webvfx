schema_version: 0.1
title: Chromix Demo Transition
version: 0.0.1
copyright: Copyright (C) 2010 Hewlett-Packard Development Company, L.P.
license: BSD-new
language: en
#url:
creator: Andrew Wason
tags:
  - Video # this may produce video
description: Simple Chromix test

#XXX specify path to HTML, and mapping of video param names
chromix.html: transition.html
chromix.a_image: srcVideo
chromix.b_image: dstVideo
# XXX extra image names from html - optimize out snapshots (in==out)
# XXX name this "producers"?
chromix.images:
  - demoSnapshot
  - demoVideo

#XXX load raw metadata, then postprocess to add identifier etc. and to generate image params (e.g. resource and passthru props including snapshot time) from image data
#XXX also process params into a map so we can lookup name and get type
#XXX map image name to the producer itself? and for snapshots remove after first set
#XXX for snapshot, user can set in==out and we just get_image once then remove from map
#XXX so user can set producer.demoSnapshot.resource=file.mov etc.
#XXX allow it to be optional? e.g. a given trans may support 3 snapshots/videos - but allow some to not be specified?
#
#XXX array of name, producer, rawimage - remove item (and close producer) when in==out
parameters:
  - identifier: title
    title: Title
    type: string
    description: Title rendered in html
    readonly: no
    required: no
    mutable: no
    widget: text
