# On MacOS, can open and build chromix.xcodeproj
# On Linux, this will add our target to ${CHROMIUM_ROOT}/src/Makefile
# so build using that.

${CHROMIUM_ROOT}/src/build/gyp_chromium --toplevel-dir=${CHROMIUM_ROOT}/src --depth=${CHROMIUM_ROOT}/src chromix.gyp
